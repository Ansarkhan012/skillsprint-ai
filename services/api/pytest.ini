[pytest]
# The workspace's legacy .pytest_cache can be inaccessible under sandboxed runs.
# Cacheprovider is only a test-result cache; disabling it does not skip fixture,
# FastAPI lifespan, HTTP client, or pytest teardown.
addopts = -p no:cacheprovider
